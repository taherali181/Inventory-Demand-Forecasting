server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing — any unmatched path falls back
    # to index.html rather than 404ing.
    location / {
        try_files $uri /index.html;
    }
}
