# Copy to .env.local (git-ignored by CRA's default .gitignore) to override.
# Base URL of the FastAPI backend. CRA only reads REACT_APP_-prefixed vars,
# and only at build/start time (no runtime reload).
REACT_APP_API_BASE_URL=http://127.0.0.1:8000
